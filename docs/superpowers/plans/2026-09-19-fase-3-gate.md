# Registro del Gate de Salida — Fase 3: RAG

**Fecha de ejecución:** 2026-09-22  
**Entorno:** Supabase local (`supabase_db_tess`), OpenRouter (REST)  
**Comando automatizado:** `pnpm gate:f3`  
**Resultado general:** ✅ **APROBADO** (19/19 criterios cumplidos)

---

## Resumen de comprobaciones

| # | Criterio de aceptación | Estado | Evidencia principal |
|---|---|---|---|
| 1 | `gate:f2` en verde: build, test, typecheck, lint, bundle < 250 kB y limpio | ✅ Cumplido | 350 tests passed, `tess.global.js` = 215.2 kB, cero lints, cero errores TS |
| 2 | Worker procesa PDF textual, Markdown y TXT | ✅ Cumplido | 45 tests en `ingest-worker` y smoke test exitoso con `smoke.pdf` |
| 3 | Un documento pasa de `pending` a `ready` sin intervención | ✅ Cumplido | `smoke.pdf` (`a4c4bfaf-a769-4a77-a988-1281babafbc9`) pasó a `ready` en BD |
| 4 | Embeddings persistidos con exactamente 1536 dimensiones | ✅ Cumplido | `extensions.vector_dims(embedding) = 1536` en `document_embeddings` |
| 5 | API y worker usan el mismo modelo de embeddings | ✅ Cumplido | `OPENROUTER_EMBEDDING_MODEL=openai/text-embedding-3-small` en ambos |
| 6 | RLS impide recuperar documentos de otro proyecto | ✅ Cumplido | 27 tests en `services/api/src/rls.test.ts` ejecutados contra DB real |
| 7 | Un visitante del proyecto A recupera solo de A | ✅ Cumplido | Validado en `src/rls.test.ts` con contexto de sesión `0013` |
| 8 | Un visitante no puede leer `document_sections` por PostgREST | ✅ Cumplido | Validado en `src/rls.test.ts` con token anónimo |
| 9 | `assistant.source` llega antes del primer `assistant.delta` | ✅ Cumplido | Verificado en `messages.route.test.ts` y stream SSE de la demo |
| 10 | `messages.sources` conserva el detalle de secciones | ✅ Cumplido | Registro JSON en BD: `[{"title":"smoke.pdf","sectionId":"...","documentId":"..."}]` |
| 11 | Fallo de RAG degrada sin citas y audita `rag.retrieval.failed` | ✅ Cumplido | Evento registrado en `audit_events` con `{"model":"openai/text-embedding-3-small"}` |
| 12 | Una fuente por documento, no una por sección | ✅ Cumplido | Deduplicación en `rag/citations.ts` y en `tess-web-component` |
| 13 | Endpoint de subida solo acepta miembros | ✅ Cumplido | 8 tests en `documents.test.ts` (404/403 a visitantes y no miembros) |
| 14 | MIME no soportado marca `failed` sin tumbar el worker | ✅ Cumplido | Tests de worker en `src/extract/index.test.ts` y validación en API |
| 15 | Rate limit de 30 mensajes / 5 minutos por conversación aplica | ✅ Cumplido | Tests en `messages.test.ts` validando código 429 tras 30 mensajes |
| 16 | CI sigue usando providers `fake` y no llama a OpenRouter | ✅ Cumplido | `.env` con `MODEL_PROVIDER=fake` y `EMBEDDING_PROVIDER=fake`, `pnpm gate:f3` verde |
| 17 | Smoke test manual con OpenRouter produce streaming real con citas | ✅ Cumplido | Claude Sonnet 5 respondió citando `smoke.pdf` con respuesta exacta |
| 18 | Smoke test verifica dimensión y registra modelo real | ✅ Cumplido | Dimensión 1536 y modelo `openai/text-embedding-3-small` en BD |
| 19 | Ingesta no ocurre dentro de HTTP (análisis de imports) | ✅ Cumplido | `scripts/check-f3.mjs` verifica que `services/api/src` no importa ingesta |

---

## Evidencia detallada por criterio

### 1. Gate automatizado (`pnpm gate:f3`)

- **Tests unitarios e integración (350 passed):**
  - `@teams4soft/tess-embeddings`: 16 passed
  - `@teams4soft/tess-client`: 24 passed
  - `@teams4soft/tess-types`: 14 passed
  - `@teams4soft/tess-core`: 21 passed
  - `@teams4soft/ingest-worker`: 45 passed
  - `@teams4soft/tess-rive`: 14 passed
  - `@teams4soft/tess-web-component`: 59 passed
  - `@teams4soft/api`: 157 passed (incluyendo los 27 tests de RLS)
- **Typecheck:** 0 errores TypeScript en todos los paquetes.
- **Lint:** 0 advertencias o errores ESLint.
- **Bundle Web Component:**
  - `dist/tess.global.js`: 215.2 kB (límite: 250 kB).
  - Comprobaciones de contenido prohibido: limpio (sin `zod`, sin `pk_live_`, sin claves de AI Gateway).

### 2, 3, 4, 5, 18. Smoke test de ingesta y embeddings (OpenRouter)

- Archivo de prueba subido: `smoke.pdf` (642 bytes, PDF textual con información de mantenimiento del reactor Zafiro).
- Estado registrado en tabla `public.documents`:
  ```sql
  id: a4c4bfaf-a769-4a77-a988-1281babafbc9
  project_id: ce7f7f9d-1c34-49ca-8f4c-504bbc698e4d
  title: smoke.pdf
  mime_type: application/pdf
  status: ready
  failure_reason: null
  ```
- Consulta de dimensiones y modelo en `public.document_embeddings`:
  ```text
               model             | dims | count 
  -------------------------------+------+-------
   openai/text-embedding-3-small | 1536 |     1
  ```

### 9, 10, 12, 17. Interacción de chat y citas en streaming

- Pregunta enviada desde el cliente:
  > *"Cual es el codigo secreto de mantenimiento del reactor Zafiro?"*
- Respuesta generada por `anthropic/claude-sonnet-5` vía OpenRouter:
  > *"El código secreto de mantenimiento del reactor Zafiro es **RQ-77 alfa**, según la documentación proporcionada."*
- Citas guardadas en `public.messages.sources`:
  ```json
  [
    {
      "title": "smoke.pdf",
      "sectionId": "8c495511-b5c8-4946-a8cd-a7d74819b699",
      "documentId": "a4c4bfaf-a769-4a77-a988-1281babafbc9"
    }
  ]
  ```
- Secuencia de eventos SSE observada:
  1. `assistant.state`: `thinking`
  2. `assistant.source`: `{"title":"smoke.pdf","documentId":"a4c4bfaf-a769-4a77-a988-1281babafbc9"}` (antes del primer delta)
  3. `assistant.state`: `speaking`
  4. `assistant.delta`: texto progresivo
  5. `assistant.completed`

### 11. Degradación elegante ante fallos de RAG

- Al configurar temporalmente una clave no válida para el embedder y enviar una consulta, la conversación no falló con `assistant.error` sino que continuó degradada.
- Registro en `public.audit_events`:
  ```text
  action: rag.retrieval.failed
  metadata: {"model": "openai/text-embedding-3-small"}
  created_at: 2026-09-22 17:28:54.352078+00
  ```

### 6, 7, 8, 13, 15. Seguridad, RLS y permisos

- `services/api/src/rls.test.ts` ejecutado contra Supabase PostgreSQL real: 27/27 tests en verde.
- La función de búsqueda `match_document_sections` (`security invoker`) y las políticas RLS garantizan aislamiento cross-tenant absoluto:
  - Un usuario/visitante de tenant A jamás recibe fragmentos de tenant B.
  - Visitantes anónimos no pueden hacer SELECT directo sobre `document_sections`.
  - Subida restringida exclusivamente a miembros autenticados del proyecto.

### 16, 19. Aislamiento de CI y comprobaciones estructurales

- `scripts/check-f3.mjs` validó:
  1. Ningún archivo en `services/api/src` importa módulos del worker o de extracción (`extract/`, `chunk.js`, `persist.js`, `ingest-worker`).
  2. Árbol de dependencias limpio de librerías propietarias de IA (`ai`, `@ai-sdk/*`, `@openrouter/*`) en `api` y `ingest-worker`.
  3. MIME types soportados consistentes entre API y worker.
  4. `permissions.ts` eliminado: la seguridad reside íntegramente en SQL / RLS.
- `.env` restablecido a `MODEL_PROVIDER=fake` y `EMBEDDING_PROVIDER=fake`.

---

## Conclusión

La Fase 3 (RAG) cumple con la totalidad de los requisitos funcionales, de seguridad y arquitecturales del spec.
