{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Tess library (agnóstico de framework, ESM, lo empaqueta tsup)",
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "types": []
  }
}
