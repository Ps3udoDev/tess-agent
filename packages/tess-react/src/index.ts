/**
 * @teams4soft/tess-react
 *
 * Wrapper React opcional sobre `<teams4soft-assistant>`.
 *
 * TODO(fase-4 o a demanda): añadir `src/Tess.tsx` con forwardRef y el puente
 * de eventos del custom element a props `on*`.
 */
export { TAG_NAME } from '@teams4soft/tess-web-component';
