/**
 * @teams4soft/tess-svelte
 *
 * Wrapper Svelte sobre `<teams4soft-assistant>`. Es una envoltura del mismo
 * núcleo, no una implementación paralela.
 *
 * TODO(fase-1): añadir `src/Tess.svelte` y migrar el build a
 * `@sveltejs/package`. Mientras no haya archivos .svelte, tsup basta.
 */
export { TAG_NAME } from '@teams4soft/tess-web-component';
